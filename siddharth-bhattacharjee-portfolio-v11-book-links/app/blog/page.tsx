import Link from 'next/link';
import { ArrowUpRight } from 'lucide-react';
import { posts } from '../../lib/posts';
import NewsletterForm from '../../components/NewsletterForm';
export const metadata={title:'Marketing Insights | Siddharth Bhattacharjee',description:'Practical marketing articles on strategy, B2B, B2C, growth, brand, product marketing, CRM, SEO and AI.'};
const clusters=['All','Marketing Strategy','B2B Marketing','Growth','Brand','Product Marketing','CRM & Retention','SEO','Consulting','Small Business'];
export default function Blog(){return <main><section className="container services-hero"><div className="eyebrow">The Marketing Brief · Insights</div><h1 className="display">Useful marketing thinking.<br/><span>For people running businesses.</span></h1><p>Practical frameworks, examples and case studies for founders, business owners and marketing teams. No generic motivational content.</p></section><section className="container section"><div className="cluster-row">{clusters.map(c=><span key={c} className="tag">{c}</span>)}</div><div className="blog-archive">{posts.map((p,i)=><Link href={`/blog/${p.slug}`} className={`archive-post ${i===0?'featured':''}`} key={p.slug}><div className="num">{p.category} · {p.readTime}</div><h2>{p.title}</h2><p>{p.excerpt}</p><div className="meta">Read article <ArrowUpRight size={14}/></div></Link>)}</div></section><section className="container section newsletter-inline"><div><div className="eyebrow">Get the useful stuff by email</div><h2 className="display">One idea.<br/>Every week.</h2></div><div><p>I&apos;ll send one practical marketing idea, framework or case study. You can unsubscribe whenever you want.</p><NewsletterForm source="blog-index" tag="Newsletter" compact/></div></section></main>}
