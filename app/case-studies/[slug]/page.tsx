import Link from 'next/link';
import {ArrowUpRight} from 'lucide-react';
import {caseStudies} from '../../../lib/case-studies';

export async function generateStaticParams(){return caseStudies.map(c=>({slug:c.slug}));}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const c=caseStudies.find(x=>x.slug===slug);return {title:c?.title||'Case Study',description:c?.summary||'Marketing case study',alternates:{canonical:`https://siddharthbhattacharjee.in/case-studies/${slug}`}};}

export default async function CaseStudy({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const c=caseStudies.find(x=>x.slug===slug);if(!c)return null;return <main className="surface-pearl commercial-page"><article className="container case-detail"><p className="label">{c.category} · {c.type}</p><h1 className="display h-xl">{c.title}</h1><p className="case-lead">{c.summary}</p><section className="case-result"><div><span className="eyebrow">The lesson</span><h2>{c.lesson}</h2></div><p>This page captures the strategic lesson and context. For first-hand work, the useful discussion is always the underlying problem, the evidence available at the time, the choices made and the trade-offs involved.</p></section><section className="case-thinking"><div><p className="eyebrow">How I approach this kind of problem</p><h2>Diagnose the constraint before prescribing the channel.</h2></div><div><p>What changed in the business? Which customer behaviour mattered? Where was the system constrained? Which alternatives were available? What evidence would support or weaken the hypothesis?</p><p>That is the same logic I use across growth, B2B demand, product marketing, positioning and lifecycle work. The channel is downstream of the diagnosis.</p></div></section><div className="case-navigation"><Link className="button alt" href="/case-studies">All case studies</Link><Link className="button" href="/#booking">Talk about your business <ArrowUpRight size={16}/></Link></div></article></main>}
