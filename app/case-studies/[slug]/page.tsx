import Link from 'next/link';
import { ArrowUpRight } from 'lucide-react';
import { caseStudies } from '../../../lib/case-studies';
export async function generateStaticParams(){return caseStudies.map(c=>({slug:c.slug}));}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const c=caseStudies.find(x=>x.slug===slug);return {title:c?.title||'Marketing Case Study',description:c?.summary};}
export default async function CaseStudy({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const c=caseStudies.find(x=>x.slug===slug);if(!c)return null;const firstHand=c.type==='Siddharth experience';return <main className="surface-pearl"><article className="container article case-study-detail"><div className="eyebrow">{c.type} · {c.category}</div><h1>{c.title}</h1><p className="lead">{c.summary}</p><div className="case-note">{firstHand?'This case study is based on work from my career. It focuses on the marketing problem, approach and lessons rather than presenting confidential company information.':'This is an original marketing analysis. It is not affiliated with or endorsed by the company discussed.'}</div><div className="prose"><h2>The problem</h2><p>Strong marketing starts with a clear business problem. In this case, the challenge was to create a better connection between customer behaviour and the business outcome that mattered.</p><h2>The approach</h2><p>I would break the problem into customer, proposition, distribution, experience and measurement. The important part is not to optimise one channel in isolation. It is to identify the constraint that is preventing the system from moving.</p><h2>What the marketing system teaches us</h2><ul><li>Start with the customer problem before choosing a channel.</li><li>Make the value proposition specific enough to guide execution.</li><li>Build a measurement loop that tells the team what to scale, change or stop.</li><li>Use brand, product, acquisition and retention as connected parts of the same customer journey.</li></ul><h2>The lesson</h2><p>{c.lesson}</p></div><div className="article-end"><Link className="button" href="/#booking">Apply this thinking to my business <ArrowUpRight size={16}/></Link><Link className="button alt" href="/case-studies">More case studies</Link></div></article></main>}
