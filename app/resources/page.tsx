import Link from 'next/link';
import {ArrowUpRight,Download} from 'lucide-react';
import {resources} from '../../lib/resources';

export const metadata={title:'Free Marketing Resources | Siddharth Bhattacharjee',description:'Practical marketing frameworks and working resources for founders, business owners and marketing teams.'};

export default function Resources(){return <main className="surface-pearl resources-page"><section className="container resources-hero"><div className="eyebrow">Free working resources</div><h1 className="display">Useful frameworks.<br/><span>Built to be used.</span></h1><p>These are the tools I use to structure marketing problems. Give me your name and email, and I&apos;ll send you a properly formatted copy you can use with your team.</p></section><section className="container section"><div className="resource-grid">{resources.map(r=><article className="resource-card" key={r.slug}><div className="num">{r.category}</div><Download size={20}/><h2>{r.title}</h2><p>{r.description}</p><Link href={`/resources/${r.slug}`}>See what&apos;s inside <ArrowUpRight size={15}/></Link></article>)}</div></section><section className="container cta"><div className="cta-box"><div><div className="eyebrow">Need a second pair of eyes?</div><h2>Use the framework. Then talk to me.</h2></div><div><p>If the worksheet makes it clear that the problem is bigger than a template can solve, send me the actual business problem.</p><a className="button" href="/#booking">Talk about your business <ArrowUpRight size={16}/></a></div></div></section></main>}
