import Link from 'next/link';
import { ArrowUpRight } from 'lucide-react';
import { posts } from '../../lib/posts';

export const metadata={title:'Marketing Insights | Siddharth Bhattacharjee',description:'Practical marketing thinking for founders, business owners and marketing leaders making growth, positioning and go-to-market decisions.'};

export default function Blog(){return <main className="surface-pearl"><section className="container section-head" style={{paddingTop:96}}><div><p className="label">Insights · How I think about growth</p><h1 className="display h-lg">Useful marketing thinking.<br/><span>For people making business decisions.</span></h1></div><p>These are not news posts or content for a publishing calendar. They are practical essays on strategy, positioning, growth, product marketing and commercial problems I regularly see in businesses.</p></section><section className="container section"><div className="blog-grid">{posts.map((p,i)=><Link href={`/blog/${p.slug}`} className={`post ${i===0?'featured':''}`} key={p.slug}><div className="num">{p.category} · {p.readTime}</div><h3>{p.title}</h3><p>{p.excerpt}</p><div className="meta">Read the thinking <ArrowUpRight size={14}/></div></Link>)}</div></section><section className="container cta"><div className="cta-box"><div><div className="eyebrow">Have a problem behind the problem?</div><h2>Good marketing starts with the right diagnosis.</h2></div><div><p>If something you read here sounds familiar, bring me the actual business problem. We can work out what is really stuck before deciding what to do about it.</p><Link className="button" href="/#booking">Talk about your business <ArrowUpRight size={16}/></Link></div></div></section></main>}