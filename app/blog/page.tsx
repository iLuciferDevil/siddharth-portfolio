import type {Metadata} from 'next';import Link from 'next/link';import {posts} from '../../lib/posts';
export const metadata:Metadata={title:'Marketing Blog',description:'Marketing strategy, growth, brand, product marketing, CRM and SEO insights by Siddharth Bhattacharjee.'};
export default function Blog(){return <main className="container section"><div className="section-head"><div><div className="eyebrow">The marketing playbook</div><h1 className="display" style={{fontSize:'clamp(52px,7vw,96px)',margin:0}}>Thinking out loud.</h1></div><p>Practical, opinionated writing about how marketing actually works. Built to be useful first, searchable second.</p></div><div className="grid3">{posts.map((p,i)=><Link className="post" key={p.slug} href={`/blog/${p.slug}`}><div className="num">{String(i+1).padStart(2,'0')} / {p.category}</div><h3>{p.title}</h3><p>{p.excerpt}</p><div className="meta">{p.readTime} · {p.date} ↗</div></Link>)}</div></main>}
