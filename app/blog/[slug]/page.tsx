import Link from 'next/link';
import { ArrowUpRight } from 'lucide-react';
import { posts,getPost } from '../../../lib/posts';
import NewsletterForm from '../../../components/NewsletterForm';
export async function generateStaticParams(){return posts.map(p=>({slug:p.slug}));}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=getPost(slug);return {title:p?.title||'Marketing Insight',description:p?.excerpt};}
export default async function PostPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=getPost(slug);if(!p)return null;return <main className="surface-pearl"><article className="container article"><div className="eyebrow">{p.category} · {p.readTime}</div><h1>{p.title}</h1><p className="lead">{p.excerpt}</p><div className="article-meta"><span>By Siddharth Bhattacharjee</span><span>{p.date}</span></div><div className="article-layout"><div className="prose">{p.content.map((block,i)=>block.type==='h2'?<h2 key={i}>{block.text}</h2>:block.type==='h3'?<h3 key={i}>{block.text}</h3>:block.type==='ul'?<ul key={i}>{block.text.split(';').map(x=><li key={x}>{x}</li>)}</ul>:<p key={i}>{block.text}</p>)}<div className="article-upgrade"><div className="num">Free resource</div><h3>Want the framework behind this?</h3><p>Get my <strong>{p.leadMagnet}</strong> and work through the problem with your own business.</p><Link href="/resources">Get the free resources <ArrowUpRight size={15}/></Link></div></div><aside className="article-sidebar"><div className="sticky-card"><div className="num">The Marketing Brief</div><h3>Useful marketing thinking. Once a week.</h3><p>Frameworks, case studies and practical ideas. No generic fluff.</p><NewsletterForm source={p.slug} tag={p.tag}/></div></aside></div><div className="article-end"><Link className="button" href="/#booking">Want help applying this? <ArrowUpRight size={16}/></Link><Link className="button alt" href="/case-studies">Read case studies</Link></div></article></main>}
