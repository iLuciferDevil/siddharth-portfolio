import Link from 'next/link';
import { ArrowUpRight } from 'lucide-react';
import { posts,getPost } from '../../../lib/posts';

export async function generateStaticParams(){return posts.map(p=>({slug:p.slug}));}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=getPost(slug);return {title:p?.title||'Marketing Insight',description:p?.excerpt};}
export default async function PostPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=getPost(slug);if(!p)return null;return <main className="surface-pearl"><article className="container" style={{maxWidth:900,padding:'96px 22px 120px'}}><p className="label">{p.category} · {p.readTime}</p><h1 className="display" style={{fontSize:'clamp(42px,6vw,76px)',lineHeight:.98,margin:'0 0 24px'}}>{p.title}</h1><p style={{fontSize:20,color:'var(--on-pearl-mute)',maxWidth:700}}>{p.excerpt}</p><div style={{display:'flex',gap:24,fontSize:13,color:'var(--on-pearl-mute)',margin:'28px 0 56px'}}> <span>By Siddharth Bhattacharjee</span><span>{p.date}</span></div><div style={{maxWidth:720,fontSize:17,lineHeight:1.8}}>{p.content.map((block,i)=>block.type==='h2'?<h2 key={i} style={{fontFamily:'Archivo',fontSize:32,lineHeight:1.1,margin:'48px 0 14px'}}>{block.text}</h2>:block.type==='h3'?<h3 key={i}>{block.text}</h3>:block.type==='ul'?<ul key={i}>{block.text.split(';').map(x=><li key={x}>{x}</li>)}</ul>:<p key={i}>{block.text}</p>)}<div style={{marginTop:60,padding:28,borderRadius:16,background:'#fff'}}><div className="num">Free resource</div><h3 style={{fontFamily:'Archivo',fontSize:28}}>Want the framework behind this?</h3><p>Get the <strong>{p.leadMagnet}</strong> and work through the problem with your own business.</p><Link href="/resources" style={{fontWeight:700,color:'var(--brass-lo)',display:'inline-flex',alignItems:'center',gap:7}}>Get the free resources <ArrowUpRight size={15}/></Link></div><div style={{marginTop:48,display:'flex',gap:12,flexWrap:'wrap'}}><Link className="button" href="/#booking">Want help applying this? <ArrowUpRight size={16}/></Link><Link className="button alt" href="/case-studies">Read case studies</Link></div></div></article></main>}